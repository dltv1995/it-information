console.log('header.js loaded: shared-sidebar-v10');
const HEADER_VERSION='shared-sidebar-v10';
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
async function init(){try{const r=await fetch(`components/header.html?v=${HEADER_VERSION}`,{cache:'no-store'});if(!r.ok)throw Error(`โหลด Header ไม่สำเร็จ ${r.status}`);const root=document.getElementById('layoutRoot');if(root)root.innerHTML=await r.text();setPage();bind();paintTheme();loadUser();}catch(e){console.error(e)}finally{document.getElementById('appBody')?.classList.remove('hidden');document.dispatchEvent(new CustomEvent('shared:layout-ready'));}}
function setPage(){const body=document.body,page=body.dataset.activeMenu||'';document.getElementById('pageTitle').textContent=body.dataset.title||'';document.getElementById('pageSubtitle').textContent=body.dataset.subtitle||'';document.querySelectorAll('.nav-item').forEach(a=>a.classList.toggle('active',a.dataset.page===page));}
function bind(){const side=document.getElementById('sidebar'),overlay=document.getElementById('mobileOverlay');const open=()=>{side.classList.remove('-translate-x-full');overlay.classList.remove('hidden')};const close=()=>{side.classList.add('-translate-x-full');overlay.classList.add('hidden')};document.getElementById('mobileMenuBtn')?.addEventListener('click',open);document.getElementById('closeSidebarBtn')?.addEventListener('click',close);overlay?.addEventListener('click',close);document.getElementById('themeToggleBtn')?.addEventListener('click',()=>{document.documentElement.classList.toggle('dark');localStorage.setItem('color-theme',document.documentElement.classList.contains('dark')?'dark':'light');paintTheme();document.dispatchEvent(new CustomEvent('shared:theme-change'));});document.getElementById('logoutBtn')?.addEventListener('click',async()=>{try{const cfg=await import('./firebase-config.js');const sdk=await import('https://www.gstatic.com/firebasejs/10.8.0/firebase-auth.js');await sdk.signOut(cfg.auth);}catch(e){}location.href='login.html';});}
function paintTheme(){const dark=document.documentElement.classList.contains('dark'),icon=document.getElementById('themeIcon');if(icon)icon.className=`fa-solid ${dark?'fa-sun':'fa-moon'}`;}
function loadUser(){const name=localStorage.getItem('user_name');if(name)document.getElementById('userName').textContent=name;const role=localStorage.getItem('user_role');if(role)document.getElementById('userRole').textContent=role;}
